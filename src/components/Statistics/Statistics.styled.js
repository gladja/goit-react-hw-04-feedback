import { styled } from 'styled-components';

export const List = styled('ul')({
  listStyle: 'none',
  padding: '0',
  textAlign: 'center'
})

export const Item = styled('li')({
  padding: '10px 0',
  fontWeight: 'bold'
})

